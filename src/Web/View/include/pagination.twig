{% if pagination %}
    {% set separator = '?' %}
    {% if '?' in extraLink %}
        {% set separator = '&' %}
    {% endif %}
    <div class="text-center">
        <nav class="btn-group">
            {% if pagination.current > 1 %}
                <a href="{{ domain }}{{ link }}{{ extraLink }}{{ separator }}p={{ pagination.current - 1 }}"
                   class="btn main-color grey-border">
                    <span aria-hidden="true">&laquo;</span>
                    <span class="sr-only">{% trans "Anterior" %}</span>
                </a>
            {% endif %}

            {% for page in pagination.pages %}
                {% if page matches '/^\\d+$/' %}
                    {% set classBtn = 'main-color grey-border' %}
                    {% if pagination.current == page %}
                        {% set classBtn = ' main-bg white-color' %}
                    {% endif %}
                    <a href="{{ domain }}{{ link }}{{ extraLink }}{{ separator }}p={{ page }}"
                       class="btn {{ classBtn }}">{{ '%02d'|format(page) }}</a>
                {% else %}
                    &nbsp;...&nbsp;
                {% endif %}
            {% endfor %}

            {% set lastPage = pagination.pages[ pagination.pages|length - 1 ] %}
            {% if pagination.current < lastPage %}
                <a href="{{ domain }}{{ link }}{{ extraLink }}{{ separator }}p={{ pagination.current + 1 }}"
                   class="btn main-color grey-border">
                    <span aria-hidden="true">&raquo;</span>
                    <span class="sr-only">{% trans "Següent" %}</span>
                </a>
            {% endif %}
        </nav>
    </div>
{% endif %}