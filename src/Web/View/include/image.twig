{% if link is defined %}
<a href="{{ domain }}{% trans %}receta{% endtrans %}/{{ link }}">
    {% endif %}
    {% set image = staticDomain ~ 'img/dummy.jpg' %}
    {% if url %}
        {% set image = url %}
    {% endif %}
    <div class="img" style="aspect-ratio: {{ aspectRatio }}">
        <img data-src="{{ image }}" class="lazyload img-cover" src=""/>
    </div>
    {% if link is defined %}
</a>
{% endif %}